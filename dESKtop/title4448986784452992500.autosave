// This tab is Title

class Title implements Scene {
  Title() {
  }

  void setup() {
  }

  void draw() {
    textAlign(CENTER, CENTER);
    textSize(100);
    fill(255);
    noStroke();
    text("START", width / 2, height / 2);
  }

  void keyPressed() {
  }

  void mousePressed() {
  }
}
